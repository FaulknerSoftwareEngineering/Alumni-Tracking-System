
-# Created by Zack Pace
-# on: March 23, 2018 
-# edited by ZP on 3/29/18
%p
  = link_to 'Add new student', new_student_path

%p
  = link_to 'home page', welcome_index_path
%h1 All students
= render :partial => 'shared/flash', :locals => { :flash => flash }
%table#student.table
  %thead
    %tr
      %th First Name
      %th Last Name
      %th Degree
      %th Grad Year
      %th Grad Term
      %th More Info

  %tbody
    - @students.each do |student|
      %tr
        %td= student.first_name
        %td= student.last_name
        %td= "#{student.earned_degrees.first.degree.name}" unless student.earned_degrees.blank?
        %td= "#{student.earned_degrees.first.year_graduated}" unless student.earned_degrees.blank?
        %td= "#{student.earned_degrees.first.season}" unless student.earned_degrees.blank?
        %td= link_to "More about #{student.first_name}", student_path(student)