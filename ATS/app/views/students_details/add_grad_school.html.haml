%h2 Add Grad School To 
= @name

= form_tag submit_add_grad_school_path(@id), :method => :get do
    = render :partial => 'grad_school_form'
    
    = submit_tag 'Add University', :class => 'btn btn-primary'
    
%div
    = render partial: 'shared/cancel_button', locals: {path: student_path(@id)}