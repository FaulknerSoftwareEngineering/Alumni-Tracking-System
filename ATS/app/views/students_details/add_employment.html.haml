%h2 Add Employer To
= @name

%div
    = form_tag submit_add_employment_path(@id), :method => :get do
        = render :partial => 'employment'
        %div
            = submit_tag 'Add Employer', :class => 'btn btn-primary'
            
        %div
            = render partial: 'shared/cancel_button', locals: {path: student_path(@id)}